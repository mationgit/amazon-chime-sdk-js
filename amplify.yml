applications:
  - appRoot: demos/browser/
    frontend:
      phases:
        preBuild:
          commands:
            - nvm install 14.17.4
            - npm install
        build:
          commands:
            - npm run start:hot
      artifacts:
        baseDirectory: /
        files:
          - '**/*'
      cache:
        paths: []
